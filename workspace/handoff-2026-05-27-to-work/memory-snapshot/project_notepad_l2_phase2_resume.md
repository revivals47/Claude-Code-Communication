---
name: project_notepad_l2_phase2_resume
description: "notepad L2 rebuild ★LAND COMPLETE 2026-05-26 + ★右クリック context-menu LAND 2026-05-27(GUI_kit #190 main f66366a + app #14 main 0a7ef26)。★threads: (0)self-paste deadlock=Paste で freeze(clipboard.rs:104 blocking pipe read を event-loop で、self Copy→Paste で source=dest 相互待ち)→ ★MERGED 2026-05-27 GUI_kit PR#191(main d35b718、clipboard.rs self-paste short-circuit + source-identity tracking、#185 案A 適用)、user verify PASS + codex design-gate/impl-review/boss1 全 PASS、★Ctrl+V crash subsume closeout (1)vector font hang=★MERGED GUI_kit PR#192(main 38f9ea8、O(N^2)→O(N) wrap_offsets、bidi/embedded-RTL→manual fallback、320x bench、user verify PASS) (2)Find dialog 表示位置バグ queue(rect-based) +tracked follow-up: cross-app paste calloop fd 非ブロック化 / vector resize per-segment cold shape(bitmap 並みに)。clipboard(#191)+vector(#192)両修正 GUI_kit main 38f9ea8 集約完了。base land=PR#189(main 7474f33)+#13(83fcab2)。follow-up=chrome× veto hook / double-triple-click / auto-scroll / worktree 掃除"
metadata: 
  node_type: memory
  type: project
  originSessionId: fc5cacde-24ff-4ed7-a350-635f44c9a94d
---

notepad の L2 (hayate-kit) consumer rebuild の進捗 (2026-05-23、[[reference_hayate_ui_shim_dep_swap]] / [[feedback_apps_on_l2]] 文脈)。

**Phase 1 (完了)**: notepad app を hayate-ui shim → hayate-kit+hayate-platform 直 dep に rebuild。branch `notepad-l2-rebuild` (GitHub remote あり、revivals47/hayate-notepad)、commit 9f3b4fe + cea0421(self-review bugfix: pending_after_save leak on save fail)。in-app FileDialog/SaveConfirm + StatusBar widget 化、native dialog.rs 撤去。

**Phase 2 widget 拡張 (完了確定、impl-review PASS)**: GUI_kit worktree `~/Documents/GUI_kit-w2-notepad`、branch `feat/notepad-l2-widgets` (base main f1f04db) に積層:
- ① 9f8db6a — TextAreaWidget Redo(text_core redo forward) + Find/Replace(SearchEngine 再利用、widget/search.rs へ relocate) + 選択 primitive。
- ② da3688d — menu_bar fidelity: MenuItem builder + MenuEnabled(Always/Fixed/Dynamic read-on-paint) + checkable/accelerator/disabled、MenuTheme に item_disabled_text/item_disabled_highlight(全7 preset)。
- Named L1 3718e57 — render/mod.rs wrapped GPU/SDF の Named→SansSerif collapse 削除(general L1 fix、cherry-pick 可)。
- ③ 6c38043 — TextArea runtime font: FontFamilyOwned + ResolvedTextStyle SSOT(render.rs 全 measure+paint site が resolved_style read、measure↔paint 対称=draw_text_with_family)、set_font/set_font_size/with_font/font()。codex impl-review **PASS** (No correctness findings)。
- 全 build green、kit 887 / platform 777、regression 0。

**Phase B (app consumer、★完了)**: `notepad-l2-rebuild` commit 6bd2ab4 (cea0421 の上)。①②③ consume:
- find_replace.rs = Find/Replace modal composite (① consume、host へ FindReplaceAction emit、Ctrl+F/Ctrl+H/F3)。
- main.rs 統合: Edit Redo(Ctrl+Y) + Undo/Redo を can_undo/can_redo の read-on-paint Cell で grayed + accelerator label + Format(WordWrap checkable + Font Sans/Serif/Mono→set_font) + Search メニュー + ② MenuItem 配線 + modal chain + find/replace action handler + per-frame refresh_menu_state。
- cargo build -j1 -p hayate-notepad 緑 (0/0)。build 時のみ Cargo.toml path-dep を `../GUI_kit-w2-notepad` に local 切替、commit は `../GUI_kit` 維持 (widget は GUI_kit main へ別途 merge=branch accumulation)。
- ★self-review で data-loss bug 捕捉+修正 (Phase1 から潜在): menu/shortcut の buffer 変更系 (Cut/Paste/Delete/TimeDate/Undo/Redo) が editor change-detection を bypass→dirty 立たず→Exit/New が save prompt skip。execute() 末尾 mark_buffer_edited() で再同期。
- ★font enumeration API 無し → Font は Format 直項目。full family-picker は L1 API (TextEngine が FontSystem.db().faces() 公開) follow-up。
- ★Phase B diff review **PASS** (codex audit + PRESIDENT converge): 3rd data-loss path なし、dirty-flag 完全 (typing/Enter/BS/Del/Ctrl-XVZ/ImeCommit=editor branch、menu-global=execute() catch-all、find/replace 明示、Paste async=editor branch)、catch-all robust、deferred-op/modal sound。**notepad Phase2 全完了確定**。

**merge**: widget branch + app とも user GUI 検証 (find/replace/undo-redo/font の cursor 整合・実 typing) 後。視覚は AI worker carve-out。

**★consolidated follow-up**:
1. ✅**modal IME plumbing — DONE (notepad-l2-rebuild d20189a、Option A、2026-05-23)**: 両 modal の `_ => Handled` swallow が IME 族を focused inner field へ forward してなかった gap を、src/modal_ime.rs の `forward_ime(event, field)` helper (IME 7 variant→focused field forward + ImeRect bubble、非IME→None) + find_replace/file_dialog 配線で解決。host/framework 変更ゼロ (App が ImeRect 読む=app.rs:2380、host が modal resp verbatim return)。async paste は ImeCommit channel 相乗りで同時修復。build -j1 緑 + scoped test 3 passed。helper は public Widget trait のみ依存ゆえ **n≥3 modal で L2 modal-base へ verbatim 昇格可** (今は app-side、n=2 から framework 抽象は premature=YAGNI)。live 日本語 IME (find/save 実打鍵+候補窓位置) は user gate。design doc: workspace/worker2-notes/modal_ime_design.md。
2. file_dialog Open-on-folder navigate (folder 選択+Open で navigate_to。現 no-op、double-click 代替)。
3. file_dialog Save 空ファイル名 feedback (現 silent no-op)。
4. Font full family-picker = L1 enumeration API (TextEngine が FontSystem.db().faces() 公開) 必要。

**★2026-05-26 land 再開 (video #188 merge 後)**: user が次 initiative に notepad 選択。★verify-first 発見 = feat/notepad-l2-widgets(HEAD 6c38043)は ★base f1f04db で現 main 192a7af から ★31 commits 遅れ(間に #187 imageview canvas[render painter-order] + #188 video[render] land)→ ★merge 前に現 main へ re-baseline 必須(notepad の render 変更=Named fix render/mod.rs + font render/text.rs が #187/#188 の render 変更と衝突想定)。land path = ①re-baseline + 再 gate(build --examples + lib test + golden) + ★consumer build 確認(hayate-notepad-l2 を rebased branch で build、widget API drift catch) → ②notepad app を rebased branch で build → ③user GUI verify(find/replace/undo-redo/font cursor 整合・実 typing・日本語 IME、AI 不可ゆえ user) → ④merge(feat/notepad-l2-widgets→GUI_kit main + app→main、video と同型: readiness→user GO→同 turn merge=[[feedback_lgtm_merge_gap_pattern]])。★**stage1(re-baseline)✅完了 + PRESIDENT 確認(2026-05-26 00:58)**: merge-main-in で現 main 192a7af 取込→ ★新 HEAD **6fc44c0**(merge commit、behind0/ahead5、origin は旧 6c38043 のまま=PR 時 push 要)。★render/ 衝突 **ZERO**(merge-tree --write-tree exit0、main render/mod.rs +201[#188 video draw_video_frame/VideoFrameSlotId + #187 painter-order record_segment] と branch +64/-21[font/Named-wrapped path + render/text.rs font SSOT]が非重複 region で 3-way auto-merge、両 intent 共存事後確認)。stray .codex 除去済。再 gate 全緑 regression0: build --all-targets(default+media、example 込)/ platform lib 793-0(+1)/ kit lib 922-0(+14 notepad test)/ golden_widgets 14-0(不変・home-PC env-drift0)/ nit=benches/inject_propagation unused-import は pre-existing 非 branch。★consumer build 緑(notepad app d20189a を ../GUI_kit-w2-notepad temp dep で build 16.49s=widget API drift 非破壊、trait-forward/re-export[menu_bar/text_area/search relocated]健全=consumer compile が forward 網羅実証、temp 切替 revert 済で app repo 残渣ゼロ)。★次段=worker2 が user GUI verify 準備(verify 用 binary[temp dep→6fc44c0 維持]+ 10 項目 checklist: typing/Find-Replace/Redo+grayed/Font+cursor-x/Named 描画/menu fidelity/WordWrap/modal 日本語 IME/data-loss 回帰/終了)→ user verify→ PASS で merge(widget→main + app、video #188 と同型、GO まで HOLD)。app worktree hayate-notepad-l2 に未追跡 PHASE2-*.md proposal 4 件(保全対象)。

**★2026-05-26 user GUI verify round (consolidated 方針=#2+#3+#4+subpixel まとめて 1 回再 verify)**: user 実機 verify で発見/要望 5 件。
- finding#1 chrome 非表示(閉じるボタン無し)= ★root cause= App::new が with_decorations(SystemLike) 未呼出 → Decorations::default()=Borderless([[reference_decorations_default_borderless]]、imageview #187 と同 class)。fix=app main.rs に build_systemlike+with_decorations(SystemLike)+titlebar_theme_win95 注入+title live。✅app commit 38d4b50、★user 確認済。
- 要望#2 デフォルト Word Wrap ON(Win95 は OFF だが user 使い勝手優先)= with_wrap(true)+wrap_checked init true。✅app 5ea6d07。
- 要望#3 wrap ON 時 横スクロールバー非表示(Win95 同様)= h_scroll_visible() helper で rect_for_text_area(HSCROLL_H reclaim)/paint/event 3 gate。✅app 4327b8d。
- 要望#4 ★縦スクロールバー常時表示(機能穴、Win95 は縦常時)= ★L2 API 追加(TextAreaWidget の scroll_y 外部化: with_scroll_y_cell/content_height/viewport_height/effective_scroll_y/write_scroll_y、横 API と 1:1 対称、§8 で wheel max_scroll を content_height 視覚行に統一=wrap 末尾到達バグ同梱是正)。codex API-gate PASS(must-fix なし、scroll-away-from-cursor は ensure_cursor_visible が paint 非呼出ゆえ snap-back せず)+ impl-review PASS(finding0、全 scroll_y site が helper 経由化網羅、boss1 grep 一致)。L2 commit ★c853786(feat/notepad-l2-widgets、6fc44c0 の上)+ app 配線 c40f9f9(常時 v_scroll + #3 h_scroll と corner 非重複)。
- ★v_scroll drag sub-pixel bug(user verify): thumb ドラッグで文字が薄く→真っ白(wheel は crisp)。root cause= drag は連続 content-px を cell 直書き(scroll_bar.rs:499、row 整列なし)→ editor が effective_scroll_y() 端数生読み→ paint が端数 Y → bitmap glyph(Win95)が AA で上下 device 行に分散=薄/白。wheel は row_height 整数倍で crisp。fix= ★effective_scroll_y() read 1 点で round(全 read 経由の唯一 choke、drag は write 非経由)。★user 判断=★案B(raw.round() 整数 px=モダン滑らか、Win95 行スナップ A 不採用)。codex gate→impl→consolidated 再 verify 進行中。
- ★**notepad 設計方針 = Win95 風 + 要所はモダン使い勝手**(user が Win95 fidelity を usability 優先で ★2 度上書き: wrap default ON / smooth scroll B)。今後の notepad UX 判断で strict Win95 を前提にしない。
- ⏸ ★chrome × の save-prompt bypass(dirty 時 × で save 確認なく即 quit)= framework gap(全 SystemLike app 共通、× は WindowAction::Close 直上げ、platform に close-request veto hook なし。menu/kbd Exit は begin_destructive で prompt)。抜本解= GUI_kit platform に close-request veto hook。★user scope 判断 未決(land 前必須 or 別 follow-up)。
- ★merge bundle 拡大: feat/notepad-l2-widgets = 原 4 commit + merge-main-in 6fc44c0 + v_scroll c853786 + subpixel fix(予定)。eventual merge gate は ★新 HEAD で build --examples+full test+golden 再実行(6fc44c0 数値流用せず、[[feedback_merge_gate_build_examples]])。2-repo merge(widget→GUI_kit main + app commits→app、video #188 同型)。
- ★**LAND COMPLETE (2026-05-26)**: 全 user finding/要望 land。最終 widget HEAD **1fc8b56** = 6fc44c0(merge-main-in 192a7af) → c853786(v_scroll L2 API、双方向+content_height、§8 wheel max_scroll 是正) → b08571b(drag-crisp、★案B effective_scroll_y raw.round() read-1点) → 1b9994a(visual_lines memoize、key=content_rev+width+wrap+style_fp[5field incl line_height]+clear-on-inject、measure-first で per-frame 2x 冗長確証) → 1fc8b56(drag-select、event_overlay capture[scroll_bar pattern]+pointer_anchor+Blur reset、app event_overlay forward additive)。app HEAD **ccd70d1**。★merge gate(1fc8b56、fresh): build --all-targets+media 緑 / platform lib 793 / kit lib 940 / golden 14 / clean FF。★2-repo merge: ★GUI_kit PR #189 MERGED(merge 7474f33、main=7474f33、9-commit 履歴保持) + ★app PR #13 MERGED(merge 83fcab2、hayate-notepad main=83fcab2、temp dep→../GUI_kit revert + landed-widget build 緑14s)。v_scroll/drag-crisp/memoize/drag-select 全 codex design-gate+impl-review+boss1/PRESIDENT 直読+consolidated user live PASS。★規律実証: measure-first(memoize で O(N²)仮説を線形に訂正) / codex gate が human-trace 漏れ catch(memoize engine-availability stale-wrap + drag-select event_overlay-capture/forward-gap) / cache-key 完全性は transitive data-flow まで trace(row_height→line_height)。★残 follow-up(本 land scope 外、別途): chrome × save-prompt veto hook(GUI_kit platform、全 SystemLike app 共通) / double-triple-click word-line select / drag-past-viewport auto-scroll / video+notepad worktree 掃除(GUI_kit-{w1,w2,w3}-video + w2-notepad、未追跡 worker-notes 保全先行=[[reference_codex_exec_background_stdin]] 隣の掃除規範、user go 待ち)。

**★2026-05-27 右クリック context-menu LAND + 新 active threads**:
- ★**context-menu LAND COMPLETE (2026-05-27)**: 右クリックで Cut/Copy/Paste/Delete/Select All の edit context menu。design 案B(L2 mechanism + L3 policy)→ codex design-gate + impl-review + boss1/PRESIDENT 直読 + user live PASS → ★2-repo merge: GUI_kit ★PR #190 MERGED(merge f66366a、main=f66366a) + app ★PR #14 MERGED(merge 0a7ef26、notepad main=0a7ef26)。L2(feat/text-context-menu 6eaae66): TextAreaWidget の PointerPress を button 判別(LEFT のみ click+drag-select、RIGHT は新 pub prepare_context_menu(x,y)=選択外なら caret 配置・選択内なら維持、pointer_selecting/anchor 不触)+ ContextMenu MenuItem に enabled(private field + new default-true + disabled() builder)+set_enabled(id,on)(grey draw + click/keyboard nav/Return skip)+ menu_bar.close() pub 化(sibling-close)。app(feat/context-menu fab5aa7、src/main.rs +107/-6): root event() で RIGHT-intercept(dialog/shortcut/scrollbar/menu-bar 後、text_area forward 前、rect_for_text_area hit-test)→ prepare_context_menu + ContextMenu popup(menu_bar.close() で single-popup)、drain_context_menu→execute(cmd)→mark_buffer_edited(dirty 即時)、event_overlay は menu_bar+context_menu+text_area forward(forward-gap class 回避)。enabled per-frame=cut/copy/delete:selection().is_some() / selectall:!text().is_empty() / paste:always(v1、clipboard-empty greying は async data-offer follow-up)。gate(6eaae66): build --all-targets+media 緑 / platform 793 / kit 950(+4 select+paste regression test) / golden 14。BC: MenuItem private enabled=repo-internal additive(da3688d 先例、外部 consumer 無)。
- ★**active thread (0) self-paste deadlock [★MERGED 2026-05-27 — GUI_kit PR #191、main d35b718]**: notepad が ★Paste で「応答なし」(繰返し freeze、force-quit)。★root-cause CONFIRMED(worker2 cargo-free trace + boss1/codex 直読): clipboard.rs paste() が ★clipboard.rs:104 read_to_string で同期 blocking pipe read(calloop event-loop スレッド)。自 Copy で selection 所有時、データ供給元 on_source_send(write)は同スレッドの Send Dispatch handler ゆえ read で block 中は走れず → ★read↔write 相互待ち=永久 deadlock。★旧 Ctrl+V crash は ★この deadlock(crash.log clean=panic 非・hang ゆえ OOB 計装 非 fire と整合、force-quit を user が crash 認識)。★root=既知 #185(DnD self-drag deadlock、wayland.rs:1261-1272 で is_self_drag→自前 data 直結 pipe 迂回=案A 根治済)の ★clipboard 未適用取りこぼし。
  ★**fix(90f99c2、feat/clipboard-self-paste-fix、main f66366a off、clipboard.rs のみ 43+/4-、API 不変)**: paste() 冒頭に ★self-paste short-circuit(pending_copy Some→text 直 return、pipe 迂回、current_offer check より前)+ ★source-identity tracking。★codex design-gate must-fix(1 高)= pending_copy 無条件 clear だと ★copy A→copy B→旧 source_A の Cancelled が pending_copy(B) clear=所有中 None で deadlock 再露出 → ★current_source field 追加、on_source_cancelled(source) は cancelled==current_source の時のみ clear、stale source は destroy のみ。pending_copy=Some ⟺ 自所有 が repeated-copy + 他 app 奪取 両方で成立。proxy 比較=wl_data_source PartialEq(object identity)。
  ★gate: codex design-gate(must-fix 1=source-identity、他 sound)+ codex impl-review(★no findings、7/7)+ boss1 直読、全 PASS。platform 783/0 + kit build + golden 14 green、pre-existing fail 不変。★user live verify PASS(自 Copy→Paste / Copy A→B→Paste[repeated-copy hole]/ Ctrl+V key + 右クリック Paste 固まらない + cross-app regression、bitmap 安定)+ codex 2nd-opinion → ★MERGED 2026-05-27: ★PR #191(merge ★d35b718、GUI_kit main=d35b718)、gate 緑(platform 783/kit 950/golden 14、FF conflict-free)。notepad app は ★commit 不要(../GUI_kit dep 自動取込、user verify 済 binary=同 fix)。★Ctrl+V crash thread は本 fix で ★subsume closeout(diag branch 0da0725 git 保全=backtrace 付き再発時のみ別件)。計装 binary は force-quit kill 済。
  ★**secondary follow-up(別 dispatch tracked、PRESIDENT 承認)**: cross-app paste は依然 clipboard.rs:104 同期 blocking read ゆえ大容量/遅 source で event-loop 一時 freeze 残 → ★calloop に pipe read fd 登録 + readable incremental read + EOF 配送で真の根治(event-loop が clipboard I/O で一切 block しない)。primary で報告 bug は解消ゆえ別 cycle。
  - (旧 active thread = 選択中 Ctrl+V crash watch): 文字選択中 Ctrl+V で intermittent「crash」(再現不可・backtrace 無)。診断計装 binary 配布(diag/select-paste-crash 0da0725、anomaly-guard が eprintln 後 自然 panic=masking 無)+ permanent regression test +4(#190 land 済)。★crash.log が panic/ANOMALY 無 = OOB panic でなく ★deadlock の徴候 → 上記 self-paste deadlock thread に ★統合(worker2 が trace で同一/別を最終判定)。
- ★**active thread (1) vector font hang [★MERGED 2026-05-27 — GUI_kit PR #192、main 38f9ea8]**: Format→Font で vector font 選択 → notepad freeze(panic でなく O(N^2) wrap)。root-cause=compute_visual_lines wrap loop が measure_prefix を 1 文字ごと growing prefix 測定=O(N^2)、vector は shape_cached が prefix 文字列 key→N cache-miss=N full cosmic shape/行(bitmap=advance 和で非 hang)。memoize は結果 cache のみで font 切替+各編集で再発。★fix=vector 分岐を TextEngine::wrap_offsets(layout_reuse 1-shape-pass→layout_runs の start-based partition)に差替、bitmap/no-wrap/manual loop(wrap_line_manual に抽出)不変。bidi(run.rtl OR intra-run glyph.start 非昇順)→None→manual fallback(LTR-monotonic のみ fast-path)。★impl ★8d554a7 + ★de99d92(test-gap 閉 + ★embedded-RTL bidi gate 実バグ修正: rtl は base direction ゆえ LTR-base 行内 embedded RTL 見逃す→intra-run 非昇順 check 追加、cosmic 0.18.2 dump[Hebrew start 降順 12,10,8,6]実測確証、conservative superset で LTR fast-path 不変)。codex design-gate + impl-review ★PASS(no must-fix、7/7)+ boss1 直読 + ★320x bench(3.53s→11ms)。platform lib 796 / kit 950 / golden 26 green。★feat/vector-wrap-perf de99d92 で ★PARKED(deadlock 最優先ゆえ)。★MERGED 2026-05-27: ⓪re-baseline(de99d92 を main d35b718 へ merge-up=d87f311、★conflict ゼロ=clipboard.rs と非重複)①codex 再 glance(de99d92 delta=intra-run 昇順 check + partition_rows pure-fn 抽出、★no findings)②build 緑(8.36s)③user verify ★PASS(『だいぶマシ・実用的な範囲』、vector フォントで resize/編集/paste 軽い、wrap behavior-change 許容、bidi 現挙動)④★GUI_kit PR #192 MERGED(merge ★38f9ea8、main=38f9ea8、gate platform 796/kit 950/golden 14、FF conflict-free)。★clipboard(#191 d35b718)+vector(#192 38f9ea8)両修正 main 集約、notepad app commit 不要。★codex 2nd-opinion follow-up(別 cycle、残)=resize 時 paint per-segment cold shape(draw_text_cached_clipped、wrap 位置変化で cache miss)=O(total segments)、本 fix 後も残る→vector resize を bitmap 並みに(shape cache の segment 非依存化 等)。
- ★**active thread (2) Find dialog 表示位置バグ [queue・最後]**: Find(Ctrl+F) dialog が ★mis-center(discoverability でなく表示位置バグ)。root cause= find_replace.rs + file_dialog.rs の parent_w/parent_h が init 0.0(layout() でのみ set)→ paint centering(px/py/overlay)が stale 0.0 read で誤位置。fix= parent_w/h を paint の rect.width/rect.height に置換(rect-based)。この 2 modal のみ該当(save/info/error は AlertDialog で parent_w 無)。worker2 が ★deadlock + vector-wrap resume 後 fresh notepad main(0a7ef26)から着手。
